export class Order {
    id?: number; // Order ID
    orderDate?: string; // Date in ISO format (e.g., '2023-10-26')
    address?: string; // Shipping address
    promocode?: string; // Promo code
  
    
  }